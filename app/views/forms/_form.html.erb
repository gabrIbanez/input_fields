<%= form_with(model: my_form) do |form| %>
  <% if my_form.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(my_form.errors.count, "error") %> prohibited this my_form from being saved:</h2>

      <ul>
        <% my_form.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div> 

  <div>
    <%= form.label :active, style: "display: block" %>
    <%= form.check_box :active %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>

